server:
  port: 8082

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/final_backend_db
    username: user
    password: pass
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update

info:
  app:
    name: ${spring.application.name}
    description: Movie Service 
    version: 1.0.0



